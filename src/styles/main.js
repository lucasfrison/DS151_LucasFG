import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    centerView: {
        justifyContent: 'center',
        alignItems: 'center',
    },
    textInput: {
        borderWidth: 1,
        width: 300,
        borderColor: 'blue',
        borderRadius: 20,
        padding: 10,
        marginTop: 10,
        marginBottom: 10
    },
    title: {
        fontSize: 32
    }
});